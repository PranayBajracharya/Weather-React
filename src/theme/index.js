import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
    colors: {
        primary: {
            default: '#282c34',
        },
        secondary: {
            default: '#eee'
        }
    },
});

export default theme;